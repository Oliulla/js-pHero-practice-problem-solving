// Write a function called make_avg() which will take 3 int and return the avg of those values
function makeAvg(a, b, c) {
    let avg = (a + b + c) / 3;
    return avg;
}
let average = makeAvg(3, 4, 5);
console.log(average);