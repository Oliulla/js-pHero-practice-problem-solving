/**
 * write a function name centimeterToMeter(). It will recieve centimeter and return meter
**/
function centimeterToMeter(c) {
    const m = c / 100;
    return m;
}
console.log(centimeterToMeter(1000));